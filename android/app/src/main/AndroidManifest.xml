<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.CAMERA" />
  <!-- FCM: Android 13+(API 33) 알림 표시 런타임 권한 -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="${usesCleartextTraffic}"
    android:supportsRtl="true">
    <!--
      FCM이 notification 페이로드를 시스템에 직접 표시할 때 쓰는 상태바 아이콘.
      상태바는 알파 채널만 읽어 단색 실루엣으로 칠하므로 ic_launcher(불투명 흰
      타일)를 쓰면 흰 사각형만 보인다. 투명 배경 흰 실루엣 전용 리소스를 지정.
    -->
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_icon"
      android:resource="@drawable/ic_notification" />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_color"
      android:resource="@color/notification_color" />
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:exported="true"
      android:theme="@style/BootTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <!--
        OAuth 콜백 딥링크(barogagiapp://oauth/...) 수신.
        Custom Tab OAuth(react-native-inappbrowser-reborn) 후 백엔드가 이 scheme으로
        302하면 OS가 이 intent-filter로 앱(singleTask)을 onNewIntent로 복귀시키고,
        RN Linking이 openAuth로 콜백 URL을 전달한다. config.ts OAUTH_CALLBACK과 일치.
      -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="barogagiapp" android:host="oauth" />
      </intent-filter>
    </activity>
  </application>
</manifest>
